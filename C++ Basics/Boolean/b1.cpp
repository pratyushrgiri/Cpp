/* we are learning about boolean */
#include <iostream>
using namespace std;
int main()
{
    int x=5;
    //for declaring boollean we use bool.
    bool aBoolean;
    /*As bool is all about 0 and 1 
    if the statement is true then it returns 1
    if the statement is false then it returns 0*/

    cout << (5==x)<<"\n"; //it prints 1 because the statement is true.
    cout << (6==x)<<"\n"; //it prints 0 because the statement is false.
    return 0;
}